package io.github.houstonclark06.aircraftperformance.model.navigationpoint;

/** Represents a waypoint on a pilot map. */
class Waypoint extends NavigationPoint {

  /**
   * Creates a Waypoint.
   *
   * @param label the label of the Waypoint
   */
  private Waypoint(String label) {
    super(label);
  }
}
